* text=auto

# Shell scripts must keep LF: CI runners and Linux containers fail on CRLF shebangs.
*.sh text eol=lf

# Container and chart files are consumed by Linux tooling only.
Dockerfile text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
nginx.conf text eol=lf
